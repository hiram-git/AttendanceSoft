{
  "name": "@attendancesoft/mobile",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 3100",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "cap sync",
    "assets": "capacitor-assets generate --iconBackgroundColor '#0a0f24' --splashBackgroundColor '#0a0f24'",
    "ios:open": "cap open ios",
    "android:open": "cap open android",
    "ios:run": "cap run ios",
    "android:run": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1"
  }
}
